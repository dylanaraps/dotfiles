#!/bin/sh

export PATH=/usr/lib/ccache/bin:~/bin:~/.local/bin:~/node_modules/.bin:$PATH
export FFF_OPENER=opn
export HISTFILESIZE=10000
export PASH_KEYID=dylan.araps@gmail.com

export CMAKE_GENERATOR=Ninja
export CFLAGS="-march=native -pipe -O3 -fno-math-errno"
export CXXLAGS="-march=native -pipe -O3 -fno-math-errno"
export LDFLAGS=""

alias gj="git add .;git commit -m 'docs: update';git push"
alias vi=vim
alias mp3="youtube-dl -x --embed-thumbnail --audio-format mp3"

paleta -r

git() {
    case $1 in commit) set -- "$@" -S; esac

    command git "$@"
}

sudo() {
    su -c "$*"
}
