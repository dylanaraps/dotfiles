#!/bin/sh

case $- in *i*) ;; *) return; esac

alias gj="git add .; git commit -m 'docs: update'; git push"
alias ls="ls --color=auto --group-directories-first"
alias xi="sudo xbps-install --repository=hostdir/binpkgs/"

cat ~/.cache/pal/colors
